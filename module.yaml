default:
  build:
    target: Vstk.Core.sln
    tool:
      name: dotnet
  install:
    - Vstk.Core/bin/Release/netstandard2.0/Vstk.Core.dll

notests *default:
  build:
    configuration: NoTests

full-build:
  build:
    configuration: Release
